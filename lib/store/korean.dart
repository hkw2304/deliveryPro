import 'package:flutter/material.dart';

class KoreanFood extends StatelessWidget {
  const KoreanFood({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(

        child: Text('한식'),
      ),
    );
  }
}
