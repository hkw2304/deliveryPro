import 'package:flutter/material.dart';

class DrinkFood extends StatelessWidget {
  const DrinkFood({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(

        child: Text('술안주'),
      ),
    );
  }
}
